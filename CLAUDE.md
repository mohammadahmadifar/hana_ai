# hana_ai — سامانه پیش‌اعتبارسنجی و پایش مجوزهای حمل‌ونقل

پروژه دانشگاهی. دو نیمه دارد که عمداً از هم جدا نگه داشته می‌شوند:

| نیمه | مسیر | نقش |
|---|---|---|
| موتور | `app/`, `dataset/`, `hana_engine/` | تولید داده مصنوعی، چاپ روی قالب، اعوجاج، پیش‌پردازش، OCR، ارزیابی |
| پنل | `panel/` | لاراول ۱۳ — لاگین، دیتاست و تگ‌گذاری، ساخت تصویر تستی، فرایند مجوز، داشبورد |

## اجرای سریع

```bash
# موتور به‌تنهایی (پایپ‌لاین اصلی پایان‌نامه)
.venv/bin/python main.py
.venv/bin/python main.py --number 10   # ده شخص تازه؛ فقط نمونه‌های جدید پردازش می‌شوند
.venv/bin/python main.py --all         # پردازش کامل از نو (وقتی الگوریتم عوض شده)

# پنل
cd panel && php artisan migrate --force && php artisan db:seed --force
# روی http://127.0.0.1:8101 بالا است (nginx: /etc/nginx/sites-enabled/hana-panel)

# کارگر صف — بدون این، OCR و تولید انبوه اجرا نمی‌شوند
nohup bash panel/scripts/queue-worker.sh > panel/storage/logs/queue.log 2>&1 &
```

پیش‌نیاز سیستمی: `tesseract-ocr` + `tesseract-ocr-fas` + `tesseract-ocr-eng`.
مسیر اجرایی Tesseract در `app/config/settings.py` خودکار حل می‌شود؛ با `TESSERACT_CMD` قابل بازنویسی است.

## قانون طلایی: پنل هرگز مستقیم به موتور دست نمی‌زند

تنها پل، کلاس `panel/app/Services/HanaEngine.php` است که `python -m hana_engine.cli` را با JSON روی stdin صدا می‌زند و JSON می‌گیرد.

```
Laravel Controller → Job (صف redis) → HanaEngine::call() → hana_engine/cli.py → app/*.py
```

- هیچ `exec`/`shell_exec` پراکنده‌ای در PHP نوشته نمی‌شود.
- هیچ سرویس HTTP پایتونی وجود ندارد. اگر روزی موتور به ماشین دیگری رفت، فقط بدنه همان یک کلاس عوض می‌شود.
- هر تماسی که ممکن است بیش از دو ثانیه طول بکشد داخل Job اجرا می‌شود، نه در چرخه درخواست.
- `hana_engine` فقط از `app/` و `dataset/` **استفاده** می‌کند؛ آن‌ها را بازنویسی نمی‌کند.

## چرا `hana_engine/layouts.py` وجود دارد

مختصات چاپ هر فیلد در `dataset/image_writer.py` هاردکد بود. در `layouts.py` به داده تبدیل شده تا:

1. هنگام ساخت هر تصویر، **کادر دور هر فیلد** را هم بدانیم — این همان برچسب آموزشی رایگان است.
2. اعوجاج‌ها با پارامتر صریح صدا زده شوند، نه تصادفی (لازمهٔ صفحهٔ ساخت تصویر تستی).

کادرها **نسبی** ذخیره می‌شوند (۰ تا ۱ نسبت به عرض و ارتفاع تصویر) تا با تغییر اندازه نشکنند.

## ساختار پنل

```
panel/
  app/Services/HanaEngine.php      تنها پل به موتور
  app/Services/Cases/              پنج مرحلهٔ فرایند مجوز، هرکدام یک کلاس
  app/Http/Controllers/Cases/CaseFromTestImageController.php
                                   پل «تصویر تستی → پروندهٔ پیش‌نویس»
  app/Jobs/ProcessCase.php         زنجیرهٔ پایپ‌لاین روی صف «ocr»
  app/Models/                      ۱۵ مدل — قرارداد داده، بدون هماهنگی عوض نشود
  routes/web.php                   فقط require می‌کند — فایل داغ
  routes/areas/*.php               هر بخش روت خودش را دارد
  config/panel_menu.php            منوی سایدبار، داده‌محور
  public/assets/app.css            سیستم طراحی مشترک — فایل داغ
  tests/Concerns/BuildsCases.php   هلپر مشترک تست‌های پرونده
  database/seeders/DemoCasesSeeder ۲۰ پروندهٔ نمایشی (جدا از DatabaseSeeder)
  scripts/queue-worker.sh          کارگر صف
  app/Console/Commands/PruneEngineFiles.php
                                   پاک‌سازی تصویرهای موقت موتور (زمان‌بندی روزانه)
  app/Console/Commands/EvaluateFieldExtraction.php
                                   دقت استخراج فیلد؛ `--last` و `--variants`
```

### نقش‌ها و مرز دسترسی

چهار نقش در `User::ROLES`. مرزشان با میان‌افزار `role:` روی گروه‌های
`routes/areas/*.php` بسته می‌شود و منوی سایدبار از `config/panel_menu.php`
همان مرز را آینه می‌کند (هر آیتم `roles` خودش را دارد).

| نقش | چه می‌بیند | چه نمی‌بیند |
|---|---|---|
| `admin` | همه‌چیز + کاربران + آستانه‌های امتیازدهی؛ تنها نقشی که پرونده و تصویر تستیِ **همه** را می‌بیند | — |
| `expert` | ویزارد پرونده، صف بررسی، اصلاح فیلد، تصمیم، گزارش خطاها، تصویر تستی | دیتاست و تگ‌گذاری |
| `data` | دیتاست، تولید انبوه، تگ‌گذاری، خروجی آموزش، تگ‌ها، تصویر تستی | پرونده‌ها و گزارش‌ها |
| `applicant` | فقط `cases/new`، مدارک و نتیجهٔ پروندهٔ **خودش** | صف بررسی، گزارش، دیتاست، تصویر تستی، مدیریت |

سه نکته که شکستنشان ساکت است:

- **نقش کف است، نه سقف.** `MediaController` دیسک `documents` را با نقش باز
  می‌کند (کارشناس باید مدرک پروندهٔ دیگران را ببیند)، ولی متقاضی همان فایل را
  با **مالکیت** باز می‌کند — `ownsFile()` روی ردیف `case_documents` تطبیق
  می‌دهد، نه با تجزیهٔ رشتهٔ مسیر، تا تغییر قرارداد نام‌گذاری این نگهبان را
  بی‌سروصدا باز نکند.
- **`cases.show` تنها روت پرونده است که میان‌افزارش سه‌نقشی است**؛ مالکیت و
  فقط‌خواندنی بودن داخل `CaseReviewController::show` کنترل می‌شود و به ویو با
  پرچم `$canReview` می‌رسد. `cases.decide` و `cases.fields.update` هرگز برای
  متقاضی باز نمی‌شوند، حتی روی پروندهٔ خودش.
- **متقاضی داشبورد جدا دارد** (`dashboard.applicant`)، نه شاخه‌ای از داشبورد
  اصلی. هر عدد داشبورد اصلی سراسری است و برای متقاضی هم بی‌معنا هم افشاگر
  است؛ کوئری‌های صفحهٔ او همه روی `user_id` خودش بسته‌اند.

ثبت‌نام عمومی وجود ندارد: حساب متقاضی را هم مدیر سامانه از
`admin/users/create` می‌سازد. `DatabaseSeeder` یک حساب برای هر چهار نقش
می‌سازد (`applicant@hana.local`).

### پایپ‌لاین پرونده — قرارداد پنج مرحله

```
DocumentPrecheck → DocumentOcr → FieldExtractor → DocumentValidator → CaseScorer
```

`CasePipeline` این پنج را به هم می‌بندد و `ProcessCase` (صف `ocr`) صدایش می‌زند.
هر مرحله **idempotent** است: اجرای دوباره نه ردیف تکراری می‌سازد نه ردیف بیات
جا می‌گذارد. اندازه‌گیری واقعی: پروندهٔ سه‌مدرکی حدود ۷.۵ ثانیه — از HA-S4 که
هر مدرک در سه بزرگ‌نمایی خوانده می‌شود، پیش از آن حدود ۳ ثانیه بود. همه‌اش
داخل صف است، پس کاربر منتظر نمی‌ماند.

شش قاعده‌ای که شکستنشان ساکت است:
- در `validation_results` ردیف `passed` **فقط** برای `scope=document|cross`
  نوشته می‌شود؛ `scope=file` فقط ایراد ثبت می‌کند. پس هر شمارش «تعداد ایراد»
  باید `whereIn('status', ['failed','warning'])` داشته باشد — یک پروندهٔ سالمِ
  تمدید ۱۳ ردیف `passed` دارد و شمارش بدون فیلتر آن را «۱۳ ایراد» می‌بیند.
- `FieldExtractor` هرگز فیلدی با `source='manual'` را بازنویسی نمی‌کند و
  `CaseScorer` تصمیمی با `decision_is_manual` را دور نمی‌ریزد. بدون این دو،
  اصلاح کارشناس با اولین پردازش دوباره پاک می‌شد.
- «بیش از حد روشن» در `DocumentPrecheck` **دو شرطی** است: روشنایی بالای
  `max_brightness` **و** کنتراست زیر `min_contrast`. سنجهٔ روشناییِ موتور
  میانگین کانال V است و برای کاغذ سفید ذاتاً بالاست — کارت ملیِ سالمِ ژنراتور
  ۲۴۰.۵ می‌گیرد در برابر سقف ۲۲۵. با شرط تک‌عاملی، هر کارت ملی رد می‌شد و هیچ
  پرونده‌ای از گام بارگذاری جلوتر نمی‌رفت. آنچه سوختن را لو می‌دهد افت کنتراست
  است (۲۲.۶ برای سالم در برابر ۱۳.۷ برای سوخته)، نه خودِ روشنایی که اشباع می‌شود.
  ⚠️ `DemoCasesSeeder` عدد روشنایی را خودش می‌نویسد و تصویر را نمی‌سنجد، پس
  دادهٔ نمایشی این دست ایرادها را نشان نمی‌دهد — با تصویر واقعیِ ژنراتور بسنج.
- **«نخواندیم» با «نداری» یکی نیست** (HA-S4). فیلد اجباری‌ای که موتور برایش
  چیزی خوانده ولی شکلش معتبر نبوده — شاهدش `ocr_runs.extra` — ایرادش `warning`
  است نه `failed`؛ مدرکِ متقاضی ناقص نیست، ما نتوانستیم بخوانیمش. تخفیف فقط
  وقتی است که **همهٔ** فیلدهای جامانده ردپا داشته باشند، وگرنه یک پلاکِ
  ناخوانا یک فیلد واقعاً غایب را پشت خودش پنهان می‌کرد.
- **تایید خودکار روی دادهٔ ندیده انجام نمی‌شود** (HA-S4، `unread_required_holds`).
  دو حالت پرونده را — حتی با امتیاز بالای آستانه — به بررسی انسانی می‌فرستند و
  **هرگز رد نمی‌کنند**: فیلد اجباریِ بی‌مقدار، و مدرک اجباری‌ای که اصلاً نیامده.
  حالت دوم بی‌سروصدا مهم‌تر بود: ردیفش `skipped` است و `CaseScorer` فقط
  `failed` و `warning` را جریمه می‌کند، پس مؤلفهٔ اعتبارسنجی دست‌نخورده ۱۰۰
  می‌ماند. اندازه‌گیری‌شده: پروندهٔ سالمی که کارت خودرویش نیامده ۹۱.۷ می‌گرفت
  و خودکار تایید می‌شد — مجوز حمل‌ونقل بدون کارت مالکیت خودرو.
- **`PrecheckIssue::notice()` امتیاز کم نمی‌کند.** «تصویر از رزولوشن مرجع
  کوچک‌تر است» را کاربر باید ببیند، ولی از وقتی OCR چندمقیاسی است همان تصویر
  کوچک معمولاً بی‌عیب خوانده می‌شود؛ جریمهٔ ۸ امتیازی برای پروندهٔ سالم غلط
  است. شدتش `warning` می‌ماند تا ویو همان ⚠️ آشنا را نشان دهد؛ تفاوت فقط
  نداشتن ردیف `validation_results` است.

آپلودها زیر `storage/app/private/documents` می‌روند و **مالکشان باید
`www-data` باشد**؛ اگر artisan را با root اجرا کنید پوشهٔ تازه با مالکیت root
و مجوز ۰۷۰۰ ساخته می‌شود و آپلود از رابط وب می‌شکند.

### مدل داده

- **مرجع:** `document_types` + `document_type_fields` (کارت ملی، گواهینامه، کارت خودرو، مجوز قبلی)، `service_types` + پیوت `service_type_document_type`
- **پرونده:** `cases` ← `case_documents` ← `ocr_runs` (پلی‌مورفیک) ← `extracted_fields` ← `validation_results` ← `score_components`
- **دیتاست:** `dataset_samples` ← `dataset_annotations`، `dataset_tags` + پیوت، `test_images`، `generation_batches`
- **تنظیمات:** `settings` — وزن‌ها و آستانه‌های امتیازدهی. **هاردکد کردن آستانه ممنوع است**، از `Setting::get()` بخوان.

مدل پرونده `PermitCase` نام دارد نه `Case`، چون `case` کلمهٔ کلیدی PHP است. جدولش `cases` است.

### فرایند مجوز (طبق فلوچارت پروژه)

```
ورود ← انتخاب خدمت (صدور | تمدید) ← آپلود مدارک لازم همان خدمت
     ← اعتبارسنجی اولیه فایل (فرمت، حجم، ابعاد، تاری، روشنایی)
     ← OCR ← استخراج فیلد ← اعتبارسنجی اسناد (اعتبار تاریخ، کامل بودن، تطابق بین مدارک)
     ← امتیاز اطمینان ← تصمیم: تایید ≥۸۰ | رد <۴۵ | بین این دو: نیاز به بررسی انسانی
```

دو چیز مستقل از امتیاز روی تصمیم اثر می‌گذارند و هر دو در «تنظیمات امتیازدهی»
قابل خاموش‌کردن‌اند: ناهمخوانی بین مدارک پرونده را **رد** می‌کند
(`cross_fail_rejects`)، و دادهٔ اجباریِ دیده‌نشده تایید خودکار را **نگه**
می‌دارد ولی هرگز رد نمی‌کند (`unread_required_holds`).

مدارک لازم هر خدمت از دیتابیس می‌آید، نه هاردکد در ویو: صدور سه مدرک، تمدید همان سه‌تا به‌علاوهٔ مجوز قبلی.

## قواعد کار روی این پروژه

- همه UI فارسی و `dir="rtl"`. رنگ ثابت ننویس — از توکن‌های `var(--...)` در `app.css` استفاده کن. تم روشن و تیره هر دو باید کار کنند.
- **هیچ مدرک هویتی واقعی** وارد مخزن، seeder یا دیتابیس نمی‌شود. فقط دادهٔ مصنوعی ژنراتور.
- آپلودها در دیسک‌های خصوصی `documents` / `dataset` / `testimages` / `exports` زیر `storage/app/private` می‌روند و فقط از مسیر محافظت‌شده با احراز هویت سرو می‌شوند — هرگز `public`.
- برچسبی که ژنراتور تولید می‌کند رایگان است و همان لحظه ذخیره می‌شود. اصلاح کارشناس روی فیلد یک پرونده هم به دیتاست برمی‌گردد.
- بدون build فرانت: نه Vite، نه npm. یک `app.css` دست‌نویس و JS ساده.

## وضعیت موتور (اندازه‌گیری ۱۴۰۵/۰۶/۰۱ پس از اسپرینت HA-S4)

روی همان ۲۵ نمونهٔ تازه، دو معیار متفاوت — و تفاوتشان مهم است:

**۱) معیار موتور** (تطابق کامل روی متن OCR، `scripts/benchmark.py`):

| مدرک | پیش از HA-S3 | پس از HA-S3 | اکنون (چندمقیاسی) |
|---|---|---|---|
| کارت ملی | ۶۷.۸ | ۹۰.۷ | **۹۶.۷** |
| گواهینامه | ۶۰.۰ | ۷۹.۲ | **۸۸.۸** |
| کارت خودرو | ۴۹.۳ | ۸۹.۶ | **۹۸.۴** |
| میانگین | ۵۹.۰ | ۸۶.۸ | **۹۴.۸** |
| CER | — | ۳.۱۰ | **۰.۷۹** |

**۲) معیار استخراج فیلد** — آنچه کاربر در صفحهٔ نتیجه می‌بیند
(`php artisan hana:evaluate-extraction --last=25 --variants`): **۹۱.۰٪**
(۳۶۴ از ۴۰۰) در برابر **۸۴.۰٪** پیش از HA-S4.

عدد دوم همیشه از اولی پایین‌تر است و باید هم باشد: معیار موتور فقط می‌پرسد
«مقدار درست جایی در متن هست؟»، ولی استخراج‌گر باید **انتخاب** کند.

معیار حالا دو عدد می‌دهد: «تطابق کامل» و **CER** (نرخ خطای کاراکتری بر پایهٔ
لونشتاین). CER=۰ دقیقاً معادل تطابق کامل است، پس دو معیار هرگز ناسازگار نمی‌شوند.

پنج ضعف قدیمی همه بسته شدند — و دو تایشان علتِ ثبت‌شده‌شان **غلط** بود:

| مشکل قدیمی | علت واقعی | نتیجه |
|---|---|---|
| پلاک ۰٪ | قالب لیبل و `format_plate` ترتیبشان یکی نبود — باگ منطقی نه OCR | ۹۰٪ |
| نام گواهینامه ۰٪ | **نه** کم‌کنتراستی گرادیان؛ سطربندی `--psm 6` وقتی مقدار با متن قالب هم‌اندازه نیست | ۸۰٪ |
| کارت ملی ضعیف‌تر | **نه** کوچک‌بودن قالب — upscale نتیجه را بدتر می‌کند (۴۵٪ در ۱x، ۱۵٪ در ۳x). همان مشکل سطربندی | کد ملی ۸۸.۹٪ |
| VIN | یک فاصلهٔ اضافه مقایسه را رد می‌کرد، نه خودِ خواندن | ۱۰۰٪ |
| چرخش بدترین اعوجاج | پیش‌پردازش deskew نداشت | دستهٔ چرخش ۴۷.۹ → ۶۸.۷ |

**درسی که به کار بعدی می‌آید:** قالب چاپ مقدار باید با متنِ خودِ قالب هم‌اندازه
باشد، وگرنه Tesseract با `--psm 6` سطربندی را می‌شکند. و مختصات `layouts.py`
باید همیشه با `dataset/image_writer.py` قفل بماند — همین ناهماهنگی بود که دقت
پلاک را صفر کرده بود.

افت شناخته‌شده: `license_number` گواهینامه ۸۶.۷ → ۷۷.۸.

### چرا OCR چندمقیاسی است (HA-S4)

موتور هر مدرک را در سه بزرگ‌نمایی نسبت به **عرض قالب مرجعش** می‌خواند
(`layouts.template_size` → ۱.۰ / ۱.۲۵ / ۱.۵) و همهٔ متن‌ها را در `variants`
برمی‌گرداند؛ `FieldExtractor` برای هر فیلد مقداری را برمی‌دارد که بیشترین
اطمینان را دارد.

دو چیز این را لازم کرد:

۱. **تصویر آپلودی رزولوشن مرجع را ندارد.** در پروندهٔ ۲۷۲ هر سه فایل از تلگرام
   آمده بودند و گواهینامه و کارت خودرو نصف عرض قالب بودند. پس «مقیاس ۱.۰»
   یعنی رساندن مدرک به رزولوشن مرجع، نه رها کردنش روی هر اندازه‌ای که آمده.
۲. **هیچ بزرگ‌نمایی ثابتی برای همهٔ فیلدها بهترین نیست.** روی همان کارت ملی،
   مقیاس ۱.۰ کد ملی را درست می‌خواند و تاریخ تولد را غلط، و ۱.۲۵ برعکس. پس
   «بهترین متن» جواب نمی‌دهد؛ انتخاب باید فیلدبه‌فیلد باشد. مقیاسِ تنهای
   ۱.۲۵ یا ۱.۵ **بدتر** از حالت قبلی است — سود از اجتماع می‌آید.

سه تلهٔ این مسیر که با عدد کشف شدند:
- عرضِ خودِ تصویر هم نامزد می‌شود، ولی **فقط وقتی از همهٔ مقیاس‌ها بزرگ‌تر
  باشد**؛ وگرنه اسکن ۳۲۰۰ پیکسلیِ یک مدرکِ ۹۶۰ پیکسلی فقط کوچک‌تر خوانده
  می‌شد، یعنی بدتر از کد قبلی.
- سقف مساحت (۱۲ مگاپیکسل) لازم است، نه فقط سقف عرض: یک PNG ۶۰۰×۵۰۰۰۰۰ از
  سقف عرض رد می‌شود و سه بار OCR شدنش چند گیگابایت حافظه می‌خواهد.
- گرد کردن عرض، نه بریدن. «۱.۵ برابرِ ۱۵۳۷» یعنی ۲۳۰۶ نه ۲۳۰۵؛ همین **یک
  پیکسل** روی ۲۵ نمونهٔ گواهینامه ۸ فیلد را عوض کرد. یعنی با ۲۵ نمونه،
  اختلاف چندواحدی در این معیار نویز است نه سیگنال.

نسخه‌های کمکی بلافاصله بعد از OCR پاک می‌شوند تا نرخ تولید فایل موقت همان
یکی در هر مدرک بماند؛ باقی‌ماندهٔ همان یکی را زمان‌بندی روزانه می‌برد
(`hana:prune-engine-files`، پیش‌فرض ۷ روز — نیازمند cron ‏`schedule:run`).

### بازتولید عدد پایه — فقط با نمونهٔ تازه

```bash
.venv/bin/python scripts/benchmark.py --number 25   # ۲۵ نمونهٔ تازه بساز و فقط همان‌ها را بسنج
.venv/bin/python scripts/benchmark.py --last 25     # بدون تولید؛ ۲۵ نمونهٔ آخر
```

`main.py` گزارشش را روی **همهٔ** فایل‌های `dataset/` می‌گیرد و همین یک بار
گمراه‌کننده شد: قالب پلاک (تسک ۶۳۷) و اندازهٔ فونت‌ها (تسک ۶۴۰) عوض شدند ولی
نمونه‌های قدیمیِ روی دیسک با لیبل‌های همان زمان ماندند. آن نمونه‌ها با موتور
امروز هرگز درست خوانده نمی‌شوند — روی آن‌ها پلاک ۴٪ و VIN ۰٪ درمی‌آید، در
حالی که روی نمونهٔ تازه هر دو ۱۰۰٪‌اند. اندازه‌گیری ۱۴۰۵/۰۶/۰۱ روی هر ۵۰
نمونه ۷۸.۶٪ داد و روی ۲۵ نمونهٔ تازه ۸۶.۸٪: هشت واحد اختلاف، صرفاً از کهنگی
فایل‌ها. `scripts/benchmark.py` بازه را صریح می‌گیرد تا این تله تکرار نشود.

`dataset/` در `.gitignore` است، پس روی هر ماشین تازه اول باید نمونه ساخته شود.

مدیریت تسک‌ها در پنل یارپی‌ام، پروژهٔ `hana`: http://pmyarcube.duckdns.org/p/hana

## سناریوهای ویدیویی تست

`docs/scenarios/` — سناریوی آزمایشگاه تستِ پنل یارپی‌ام (پروژهٔ `hana`):

| فایل | چه چیزی را ثابت می‌کند |
|---|---|
| `ha-s2.json` | دیتاست و تگ‌ها، تولید انبوه، تگ‌گذاری تصویری، خروجی آموزش، تصویر تستی با اعوجاج کنترل‌شده |
| `ha-s3.json` | کل فلوچارت مجوز: ویزارد، اعتبارسنجی فایل، OCR، استخراج، اعتبارسنجی اسناد، امتیاز، بررسی انسانی، داشبورد و گزارش |

هر دو `mode: tutorial` با زیرنویس فارسی روی ویدیو. اجرای محلی بدون عبور از صف پنل:

```bash
HANA_ADMIN_EMAIL=admin@hana.local HANA_ADMIN_PASSWORD=... \
  bash docs/scenarios/run-local.sh docs/scenarios/ha-s3.json /tmp/run-s3
# SCEN_DRY=1 → بدون ویدیو و بدون مکث، برای پیدا کردن سلکتور خراب
```

اسکریپت `base_url`/`key` فایل را به `baseUrl`/`id` که موتور می‌خواهد ترجمه می‌کند.
فایل‌های نمونهٔ مدرک (خروجی مصنوعی ژنراتور، قانون ۱۲۹) در
`pmyarcube/storage/app/test-fixtures/hana-*.png` است.
