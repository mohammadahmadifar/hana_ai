# hana_ai — سامانه پیش‌اعتبارسنجی و پایش مجوزهای حمل‌ونقل

پروژه دانشگاهی. دو نیمه دارد که عمداً از هم جدا نگه داشته می‌شوند:

| نیمه | مسیر | نقش |
|---|---|---|
| موتور | `app/`, `dataset/`, `hana_engine/` | تولید داده مصنوعی، چاپ روی قالب، اعوجاج، پیش‌پردازش، OCR، ارزیابی |
| پنل | `panel/` | لاراول ۱۳ — لاگین، دیتاست و تگ‌گذاری، ساخت تصویر تستی، فرایند مجوز، داشبورد |

## اجرای سریع

```bash
# موتور به‌تنهایی (پایپ‌لاین اصلی پایان‌نامه)
.venv/bin/python main.py
.venv/bin/python main.py --number 10   # ده شخص تازه؛ فقط نمونه‌های جدید پردازش می‌شوند
.venv/bin/python main.py --all         # پردازش کامل از نو (وقتی الگوریتم عوض شده)

# پنل
cd panel && php artisan migrate --force && php artisan db:seed --force
# روی http://127.0.0.1:8101 بالا است (nginx: /etc/nginx/sites-enabled/hana-panel)

# کارگر صف — بدون این، OCR و تولید انبوه اجرا نمی‌شوند
nohup bash panel/scripts/queue-worker.sh > panel/storage/logs/queue.log 2>&1 &
```

پیش‌نیاز سیستمی: `tesseract-ocr` + `tesseract-ocr-fas` + `tesseract-ocr-eng`.
مسیر اجرایی Tesseract در `app/config/settings.py` خودکار حل می‌شود؛ با `TESSERACT_CMD` قابل بازنویسی است.

## قانون طلایی: پنل هرگز مستقیم به موتور دست نمی‌زند

تنها پل، کلاس `panel/app/Services/HanaEngine.php` است که `python -m hana_engine.cli` را با JSON روی stdin صدا می‌زند و JSON می‌گیرد.

```
Laravel Controller → Job (صف redis) → HanaEngine::call() → hana_engine/cli.py → app/*.py
```

- هیچ `exec`/`shell_exec` پراکنده‌ای در PHP نوشته نمی‌شود.
- هیچ سرویس HTTP پایتونی وجود ندارد. اگر روزی موتور به ماشین دیگری رفت، فقط بدنه همان یک کلاس عوض می‌شود.
- هر تماسی که ممکن است بیش از دو ثانیه طول بکشد داخل Job اجرا می‌شود، نه در چرخه درخواست.
- `hana_engine` فقط از `app/` و `dataset/` **استفاده** می‌کند؛ آن‌ها را بازنویسی نمی‌کند.

## چرا `hana_engine/layouts.py` وجود دارد

مختصات چاپ هر فیلد در `dataset/image_writer.py` هاردکد بود. در `layouts.py` به داده تبدیل شده تا:

1. هنگام ساخت هر تصویر، **کادر دور هر فیلد** را هم بدانیم — این همان برچسب آموزشی رایگان است.
2. اعوجاج‌ها با پارامتر صریح صدا زده شوند، نه تصادفی (لازمهٔ صفحهٔ ساخت تصویر تستی).

کادرها **نسبی** ذخیره می‌شوند (۰ تا ۱ نسبت به عرض و ارتفاع تصویر) تا با تغییر اندازه نشکنند.

## ساختار پنل

```
panel/
  app/Services/HanaEngine.php      تنها پل به موتور
  app/Services/Cases/              پنج مرحلهٔ فرایند مجوز، هرکدام یک کلاس
  app/Jobs/ProcessCase.php         زنجیرهٔ پایپ‌لاین روی صف «ocr»
  app/Models/                      ۱۵ مدل — قرارداد داده، بدون هماهنگی عوض نشود
  routes/web.php                   فقط require می‌کند — فایل داغ
  routes/areas/*.php               هر بخش روت خودش را دارد
  config/panel_menu.php            منوی سایدبار، داده‌محور
  public/assets/app.css            سیستم طراحی مشترک — فایل داغ
  tests/Concerns/BuildsCases.php   هلپر مشترک تست‌های پرونده
  database/seeders/DemoCasesSeeder ۲۰ پروندهٔ نمایشی (جدا از DatabaseSeeder)
  scripts/queue-worker.sh          کارگر صف
```

### پایپ‌لاین پرونده — قرارداد پنج مرحله

```
DocumentPrecheck → DocumentOcr → FieldExtractor → DocumentValidator → CaseScorer
```

`CasePipeline` این پنج را به هم می‌بندد و `ProcessCase` (صف `ocr`) صدایش می‌زند.
هر مرحله **idempotent** است: اجرای دوباره نه ردیف تکراری می‌سازد نه ردیف بیات
جا می‌گذارد. اندازه‌گیری واقعی: پروندهٔ سه‌مدرکی حدود ۳ ثانیه.

دو قاعده‌ای که شکستنشان ساکت است:
- در `validation_results` ردیف `passed` **فقط** برای `scope=document|cross`
  نوشته می‌شود؛ `scope=file` فقط ایراد ثبت می‌کند. پس هر شمارش «تعداد ایراد»
  باید `whereIn('status', ['failed','warning'])` داشته باشد — یک پروندهٔ سالمِ
  تمدید ۱۳ ردیف `passed` دارد و شمارش بدون فیلتر آن را «۱۳ ایراد» می‌بیند.
- `FieldExtractor` هرگز فیلدی با `source='manual'` را بازنویسی نمی‌کند و
  `CaseScorer` تصمیمی با `decision_is_manual` را دور نمی‌ریزد. بدون این دو،
  اصلاح کارشناس با اولین پردازش دوباره پاک می‌شد.

آپلودها زیر `storage/app/private/documents` می‌روند و **مالکشان باید
`www-data` باشد**؛ اگر artisan را با root اجرا کنید پوشهٔ تازه با مالکیت root
و مجوز ۰۷۰۰ ساخته می‌شود و آپلود از رابط وب می‌شکند.

### مدل داده

- **مرجع:** `document_types` + `document_type_fields` (کارت ملی، گواهینامه، کارت خودرو، مجوز قبلی)، `service_types` + پیوت `service_type_document_type`
- **پرونده:** `cases` ← `case_documents` ← `ocr_runs` (پلی‌مورفیک) ← `extracted_fields` ← `validation_results` ← `score_components`
- **دیتاست:** `dataset_samples` ← `dataset_annotations`، `dataset_tags` + پیوت، `test_images`، `generation_batches`
- **تنظیمات:** `settings` — وزن‌ها و آستانه‌های امتیازدهی. **هاردکد کردن آستانه ممنوع است**، از `Setting::get()` بخوان.

مدل پرونده `PermitCase` نام دارد نه `Case`، چون `case` کلمهٔ کلیدی PHP است. جدولش `cases` است.

### فرایند مجوز (طبق فلوچارت پروژه)

```
ورود ← انتخاب خدمت (صدور | تمدید) ← آپلود مدارک لازم همان خدمت
     ← اعتبارسنجی اولیه فایل (فرمت، حجم، ابعاد، تاری، روشنایی)
     ← OCR ← استخراج فیلد ← اعتبارسنجی اسناد (اعتبار تاریخ، کامل بودن، تطابق بین مدارک)
     ← امتیاز اطمینان ← تصمیم: تایید ≥۸۰ | رد <۴۵ | بین این دو: نیاز به بررسی انسانی
```

مدارک لازم هر خدمت از دیتابیس می‌آید، نه هاردکد در ویو: صدور سه مدرک، تمدید همان سه‌تا به‌علاوهٔ مجوز قبلی.

## قواعد کار روی این پروژه

- همه UI فارسی و `dir="rtl"`. رنگ ثابت ننویس — از توکن‌های `var(--...)` در `app.css` استفاده کن. تم روشن و تیره هر دو باید کار کنند.
- **هیچ مدرک هویتی واقعی** وارد مخزن، seeder یا دیتابیس نمی‌شود. فقط دادهٔ مصنوعی ژنراتور.
- آپلودها در دیسک‌های خصوصی `documents` / `dataset` / `testimages` / `exports` زیر `storage/app/private` می‌روند و فقط از مسیر محافظت‌شده با احراز هویت سرو می‌شوند — هرگز `public`.
- برچسبی که ژنراتور تولید می‌کند رایگان است و همان لحظه ذخیره می‌شود. اصلاح کارشناس روی فیلد یک پرونده هم به دیتاست برمی‌گردد.
- بدون build فرانت: نه Vite، نه npm. یک `app.css` دست‌نویس و JS ساده.

## وضعیت موتور (اندازه‌گیری ۱۴۰۵/۰۵/۳۰ پس از اسپرینت HA-S3)

روی نمونه‌های تازه‌ای که با قالب‌های اصلاح‌شده ساخته شده‌اند:

| مدرک | پیش از HA-S3 | اکنون |
|---|---|---|
| کارت ملی | ۶۷.۸ | **۹۸.۳** |
| گواهینامه | ۶۰.۰ | **۷۶.۰** |
| کارت خودرو | ۴۹.۳ | **۸۶.۰** |
| میانگین | ۵۹.۰ | **۸۶.۸** |

معیار حالا دو عدد می‌دهد: «تطابق کامل» و **CER** (نرخ خطای کاراکتری بر پایهٔ
لونشتاین). CER=۰ دقیقاً معادل تطابق کامل است، پس دو معیار هرگز ناسازگار نمی‌شوند.

پنج ضعف قدیمی همه بسته شدند — و دو تایشان علتِ ثبت‌شده‌شان **غلط** بود:

| مشکل قدیمی | علت واقعی | نتیجه |
|---|---|---|
| پلاک ۰٪ | قالب لیبل و `format_plate` ترتیبشان یکی نبود — باگ منطقی نه OCR | ۹۰٪ |
| نام گواهینامه ۰٪ | **نه** کم‌کنتراستی گرادیان؛ سطربندی `--psm 6` وقتی مقدار با متن قالب هم‌اندازه نیست | ۸۰٪ |
| کارت ملی ضعیف‌تر | **نه** کوچک‌بودن قالب — upscale نتیجه را بدتر می‌کند (۴۵٪ در ۱x، ۱۵٪ در ۳x). همان مشکل سطربندی | کد ملی ۸۸.۹٪ |
| VIN | یک فاصلهٔ اضافه مقایسه را رد می‌کرد، نه خودِ خواندن | ۱۰۰٪ |
| چرخش بدترین اعوجاج | پیش‌پردازش deskew نداشت | دستهٔ چرخش ۴۷.۹ → ۶۸.۷ |

**درسی که به کار بعدی می‌آید:** قالب چاپ مقدار باید با متنِ خودِ قالب هم‌اندازه
باشد، وگرنه Tesseract با `--psm 6` سطربندی را می‌شکند. و مختصات `layouts.py`
باید همیشه با `dataset/image_writer.py` قفل بماند — همین ناهماهنگی بود که دقت
پلاک را صفر کرده بود.

افت شناخته‌شده: `license_number` گواهینامه ۸۶.۷ → ۷۷.۸.

مدیریت تسک‌ها در پنل یارپی‌ام، پروژهٔ `hana`: http://pmyarcube.duckdns.org/p/hana
