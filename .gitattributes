# ---------------------------------------------
# پایان خط
#
# مخزن روی ویندوز هم کلون می‌شود. اگر اسکریپت‌های شل CRLF بگیرند،
# داخل ظرف لینوکسی با «bad interpreter» می‌ترکند. این چند خط نمی‌گذارد
# گیت دست به آن‌ها بزند.
# ---------------------------------------------

*.sh        text eol=lf
*.py        text eol=lf
*.conf      text eol=lf
*.ini       text eol=lf
Dockerfile  text eol=lf
*.yml       text eol=lf

*.png binary
*.ttf binary
